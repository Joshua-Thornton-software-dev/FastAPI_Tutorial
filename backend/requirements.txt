# Core FastAPI & ASGI
fastapi==0.115.0
uvicorn[standard]==0.30.6

# HTTP client for Alpha Vantage API
httpx==0.27.2

# Database
SQLAlchemy==2.0.35
# psycopg2-binary==2.9.9
alembic==1.13.2

# Pydantic (already a FastAPI dep, but pin for safety)
pydantic==2.9.2
pydantic-settings==2.5.2  # For loading .env

# WebSockets & real-time
websockets==13.1

# Optional: Rate limiting (Alpha Vantage has limits)
# slowapi==0.1.9

# Dev / Testing
pytest==8.3.3
pytest-asyncio==0.24.0